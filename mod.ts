/**
 * # Utility for sorting arrays.
 *
 * Helpers for sorting arrays with {@linkcode Array.prototype.sort} and {@linkcode Array.prototype.toSorted}
 * @module
 */

export type { Comparable, ComparatorSelector } from "./types.ts";
export { createComparator } from "./createComparator.ts";
